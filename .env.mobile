# PropertyPal Mobile App - Environment Configuration
# Copy this to your mobile device or use these values in the mobile app settings

# Backend API Configuration
# When running with Docker, use your machine's IP address on the local network

# MOBILE_API_URL - The URL your mobile device will use to connect to the backend
# Find your machine's IP address:
#   Mac/Linux: ifconfig | grep "inet " | grep -v 127.0.0.1
#   Windows: ipconfig

# Examples:
# For physical device on same WiFi: http://192.168.1.100:5008/api
# For Android emulator: http://10.0.2.2:5008/api
# For iOS simulator: http://localhost:5008/api

# Replace 192.168.1.100 with YOUR machine's actual IP address
MOBILE_API_URL=http://192.168.1.100:5008/api

# Backend port (should match docker-compose BACKEND_PORT)
BACKEND_PORT=5008

# For production deployment
PRODUCTION_API_URL=https://your-domain.com/api
