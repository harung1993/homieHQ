# PropertyPal Mobile - Architecture Overview

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    MOBILE APP (React Native)                 │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ LoginScreen  │  │ HomeScreen   │  │ Properties   │ ...  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
│         │                  │                  │               │
│         └──────────────────┼──────────────────┘               │
│                            │                                  │
│                  ┌─────────▼─────────┐                       │
│                  │   AuthContext     │ ◄─── Global State    │
│                  │  (useAuth hook)   │                       │
│                  └─────────┬─────────┘                       │
│                            │                                  │
│                  ┌─────────▼─────────┐                       │
│                  │   API Service     │ ◄─── All API Calls   │
│                  │  (apiService)     │                       │
│                  └─────────┬─────────┘                       │
│                            │                                  │
│         ┌──────────────────┼──────────────────┐              │
│         │                  │                  │              │
│  ┌──────▼──────┐  ┌───────▼────────┐  ┌─────▼──────┐       │
│  │  Interceptor│  │  Token Manager │  │  Storage   │       │
│  │  (Auto Auth)│  │  (Auto Refresh)│  │ (AsyncS... │       │
│  └──────┬──────┘  └───────┬────────┘  └─────┬──────┘       │
│         │                  │                  │              │
│         └──────────────────┴──────────────────┘              │
│                            │                                  │
└────────────────────────────┼──────────────────────────────────┘
                             │
                    ┌────────▼────────┐
                    │   HTTP/HTTPS    │
                    │   (axios)       │
                    └────────┬────────┘
                             │
┌────────────────────────────▼──────────────────────────────────┐
│                    BACKEND (Flask)                             │
├───────────────────────────────────────────────────────────────┤
│                                                                │
│  ┌──────────────────────────────────────────────────────┐    │
│  │              API Endpoints (/api/...)                 │    │
│  ├──────────────────────────────────────────────────────┤    │
│  │  /auth/login          - Authenticate user             │    │
│  │  /auth/register       - Create new user               │    │
│  │  /auth/refresh        - Refresh access token          │    │
│  │  /auth/me             - Get current user              │    │
│  │                                                        │    │
│  │  /properties          - List properties (GET)         │    │
│  │  /properties/:id      - Get/Update/Delete property    │    │
│  │                                                        │    │
│  │  /maintenance         - List tasks (GET)              │    │
│  │  /maintenance/:id     - Get/Update/Delete task        │    │
│  │                                                        │    │
│  │  /finances/transactions - Get transactions            │    │
│  │  /finances/summary     - Get financial summary        │    │
│  │                                                        │    │
│  │  /tenants             - List tenants (GET)            │    │
│  │  /tenants/:id         - Get/Update/Delete tenant      │    │
│  └──────────────────────────────────────────────────────┘    │
│                            │                                   │
│  ┌────────────────────────▼────────────────────────────┐     │
│  │         JWT Authentication Middleware                │     │
│  └────────────────────────┬────────────────────────────┘     │
│                            │                                   │
│  ┌────────────────────────▼────────────────────────────┐     │
│  │              Database (SQLite/PostgreSQL)            │     │
│  │  - Users                                             │     │
│  │  - Properties                                        │     │
│  │  - Maintenance                                       │     │
│  │  - Finances                                          │     │
│  │  - Tenants                                           │     │
│  └──────────────────────────────────────────────────────┘    │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow

### 1. Authentication Flow

```
User Login
    │
    ├─► Enter email/password in LoginScreen
    │
    ├─► Call: apiService.auth.login(email, password)
    │
    ├─► POST /api/auth/login
    │
    ├─► Backend validates credentials
    │
    ├─► Returns: { access_token, refresh_token, user }
    │
    ├─► Store tokens in AsyncStorage
    │
    ├─► Update AuthContext state
    │
    └─► Navigate to Home Screen
```

### 2. API Request Flow (Authenticated)

```
User Action (e.g., view properties)
    │
    ├─► Component: apiService.properties.getAll()
    │
    ├─► Request Interceptor
    │   └─► Add Authorization: Bearer {token}
    │
    ├─► GET /api/properties
    │
    ├─► Backend validates JWT token
    │
    ├─► Returns property data
    │
    ├─► Response Interceptor
    │   └─► If 401: Auto refresh token & retry
    │
    └─► Update component state with data
```

### 3. Token Refresh Flow

```
API Request returns 401
    │
    ├─► Response Interceptor catches error
    │
    ├─► Check if refresh token exists
    │
    ├─► POST /api/auth/refresh (with refresh_token)
    │
    ├─► Backend validates refresh token
    │
    ├─► Returns new access_token
    │
    ├─► Save new token to AsyncStorage
    │
    ├─► Retry original request with new token
    │
    └─► Return data to component

If refresh fails:
    └─► Clear all tokens
    └─► Navigate to Login Screen
```

## 📂 File Structure

```
mobile-app/
├── src/
│   ├── config/
│   │   └── api.js                  ◄─ API URLs & endpoints
│   │
│   ├── services/
│   │   └── api.js                  ◄─ API service layer
│   │
│   ├── context/
│   │   └── AuthContext.js          ◄─ Auth state management
│   │
│   ├── screens/
│   │   ├── LoginScreen.js          ◄─ Login UI
│   │   ├── ServerSettingsScreen.js ◄─ Server config UI
│   │   ├── HomeScreen.js
│   │   ├── PropertiesScreen.js
│   │   ├── FinancesScreen.js
│   │   ├── TenantsScreen.js
│   │   └── SettingsScreen.js
│   │
│   ├── components/
│   │   ├── PropertyCard.js
│   │   ├── ListItem.js
│   │   └── ...
│   │
│   ├── navigation/
│   │   └── AppNavigator.js
│   │
│   └── styles/
│       └── theme.js
│
├── App.js
├── package.json
└── app.json
```

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────┐
│         Security Layers                      │
├─────────────────────────────────────────────┤
│                                              │
│  Layer 1: AsyncStorage (Encrypted on device)│
│  └─ Stores: access_token, refresh_token     │
│                                              │
│  Layer 2: Request Interceptor                │
│  └─ Adds: Authorization header              │
│                                              │
│  Layer 3: HTTPS (Production)                 │
│  └─ Encrypts: All network traffic           │
│                                              │
│  Layer 4: JWT Validation (Backend)           │
│  └─ Validates: Token signature & expiry     │
│                                              │
│  Layer 5: Auto Logout                        │
│  └─ Clears: Tokens on failed refresh        │
│                                              │
└─────────────────────────────────────────────┘
```

## 🌐 Network Configuration

### Development Setup

```
┌─────────────────────────────────────────────┐
│  Testing Platform    │  API URL              │
├─────────────────────────────────────────────┤
│  iOS Simulator       │  localhost:5000       │
│  Android Emulator    │  10.0.2.2:5000        │
│  Physical Device     │  192.168.x.x:5000     │
│  Custom (Settings)   │  User-configured      │
└─────────────────────────────────────────────┘
```

### Production Setup

```
┌─────────────────────────────────────────────┐
│  Environment  │  API URL                     │
├─────────────────────────────────────────────┤
│  Production   │  https://api.propertypal.com │
│  Staging      │  https://staging-api...      │
│  Development  │  http://[local-ip]:5000      │
└─────────────────────────────────────────────┘
```

## 🎯 State Management

```
┌────────────────────────────────────┐
│        Global State (Context)       │
├────────────────────────────────────┤
│                                     │
│  AuthContext                        │
│  ├─ user (object)                  │
│  ├─ isAuthenticated (boolean)      │
│  ├─ loading (boolean)               │
│  ├─ login(email, password)          │
│  ├─ logout()                        │
│  └─ updateUser()                    │
│                                     │
└────────────────────────────────────┘
        │
        ├─► Used by: LoginScreen
        ├─► Used by: HomeScreen
        ├─► Used by: All protected screens
        └─► Used by: Navigation logic
```

## 📊 API Service Methods

```javascript
apiService
├── auth
│   ├── login(email, password)
│   ├── register(userData)
│   ├── logout()
│   ├── getCurrentUser()
│   └── forgotPassword(email)
│
├── properties
│   ├── getAll()
│   ├── getById(id)
│   ├── create(data)
│   ├── update(id, data)
│   └── delete(id)
│
├── maintenance
│   ├── getAll()
│   ├── getById(id)
│   ├── create(data)
│   ├── update(id, data)
│   └── delete(id)
│
├── finances
│   ├── getTransactions()
│   ├── getSummary()
│   ├── getIncome()
│   └── getExpenses()
│
├── tenants
│   ├── getAll()
│   ├── getById(id)
│   ├── create(data)
│   ├── update(id, data)
│   └── delete(id)
│
└── settings
    ├── get()
    ├── update(data)
    ├── setCustomApiUrl(url)
    ├── getCustomApiUrl()
    └── clearCustomApiUrl()
```

## 🚀 Deployment Flow

```
Development
    │
    ├─► Test locally with Expo Go
    │
    ├─► Build with EAS Build
    │
    ├─► Submit to App Stores
    │
    └─► Production Release

Backend must be:
    ├─► Deployed to cloud (AWS, Heroku, etc.)
    ├─► Using HTTPS
    ├─► Using production database
    └─► CORS configured for mobile app
```

---

This architecture provides a **secure, scalable, and maintainable** foundation for your PropertyPal mobile app! 🏗️
